package ch.epfl.cs107.play.game.arpg.actor;

public enum DamageType {
    /**
     * There exists three types of damage so far,
     * physical, magic and fire.
     */
    PHYSICAL(),
    MAGIC(),
    FIRE();
}
